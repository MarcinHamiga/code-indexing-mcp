{
  "note": "Generated by ts/packages/server/scripts/write_tool_schema_parity.py.",
  "tools": [
    {
      "name": "analyze_refactor",
      "title": "Analyze refactor impact",
      "description": "Analyze a proposed rename or signature change without editing source files, for a Python, JavaScript, TypeScript, or TSX declaration. Returns required edits, likely changes, dynamic-review findings, and evidence: for a rename, resolved aliases that need no spelling change; for a signature change, compatible call sites that need no argument edit. Always read `completeness` and `limitations`: only the state 'complete' means every indexed file was analyzed, and edits should use edit_start_byte/edit_end_byte, which cover just the identifier, rather than the wider reference range.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "$defs": {
          "DeclarationSelector": {
            "description": "One declaration identity, by chunk id or its stable source location.",
            "properties": {
              "chunk_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Chunk id from a search_code or find_symbol hit. Cannot be combined with project, path, or qualified_symbol -- provide this alone, or all three of them."
              },
              "project": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Project id, name, or path. Required with path and qualified_symbol."
              },
              "path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Repo-relative POSIX path to the file holding the declaration (forward slashes, relative to the project root). Required with project and qualified_symbol."
              },
              "qualified_symbol": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Dotted qualified symbol name, e.g. 'Outer.method'. Required with project and path."
              }
            },
            "type": "object"
          },
          "ParameterShape": {
            "properties": {
              "name": {
                "description": "Parameter name.",
                "type": "string"
              },
              "kind": {
                "description": "One of positional_only, positional, keyword_only, variadic (*args-style), or keyword_variadic (**kwargs-style).",
                "enum": [
                  "positional_only",
                  "positional",
                  "keyword_only",
                  "variadic",
                  "keyword_variadic"
                ],
                "type": "string"
              },
              "required": {
                "description": "Whether this parameter has no default value.",
                "type": "boolean"
              },
              "position": {
                "description": "Zero-based position among this signature's parameters.",
                "type": "integer"
              },
              "destructured": {
                "default": false,
                "description": "Whether this slot is a destructured pattern (e.g. `{ a, b }`) collapsed to one positional parameter; if true, `name` is a synthesized label, not authoritative.",
                "type": "boolean"
              }
            },
            "required": [
              "name",
              "kind",
              "required",
              "position"
            ],
            "type": "object"
          },
          "RenameOperation": {
            "properties": {
              "kind": {
                "const": "rename",
                "default": "rename",
                "description": "Discriminator; always 'rename'.",
                "type": "string"
              },
              "new_name": {
                "description": "The declaration's new name.",
                "type": "string"
              }
            },
            "required": [
              "new_name"
            ],
            "type": "object"
          },
          "SignatureChangeOperation": {
            "properties": {
              "kind": {
                "const": "signature_change",
                "default": "signature_change",
                "description": "Discriminator; always 'signature_change'.",
                "type": "string"
              },
              "parameters": {
                "description": "The declaration's proposed new full parameter list, in order.",
                "items": {
                  "$ref": "#/$defs/ParameterShape"
                },
                "type": "array"
              }
            },
            "required": [
              "parameters"
            ],
            "type": "object"
          }
        },
        "properties": {
          "selector": {
            "$ref": "#/$defs/DeclarationSelector",
            "description": "Declaration selected by chunk id or stable source location."
          },
          "operation": {
            "description": "Discriminated rename or signature-change operation.",
            "discriminator": {
              "mapping": {
                "rename": "#/$defs/RenameOperation",
                "signature_change": "#/$defs/SignatureChangeOperation"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/RenameOperation"
              },
              {
                "$ref": "#/$defs/SignatureChangeOperation"
              }
            ]
          },
          "limit": {
            "default": 500,
            "description": "Maximum findings per page.",
            "maximum": 500,
            "minimum": 1,
            "type": "integer"
          },
          "cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Opaque analysis page cursor."
          }
        },
        "required": [
          "selector",
          "operation"
        ],
        "type": "object"
      }
    },
    {
      "name": "file_outline",
      "title": "File outline",
      "description": "List the symbols declared in one indexed file, in source order, with kind, qualified name, parent, and line range. Returns structure metadata only, never code text, so it is the cheap way to understand a file before fetching parts of it. The file must already be indexed; a root that is not registered yet is registered and indexed first, and a changed index is refreshed before the outline is returned.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "path": {
            "description": "File path relative to the project root, using forward slashes, exactly as reported in search_code hits.",
            "type": "string"
          },
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      }
    },
    {
      "name": "find_references",
      "title": "Find references",
      "description": "Find structural uses of one Python, JavaScript, TypeScript, or TSX declaration; other languages return UNSUPPORTED_LANGUAGE. Select it with a chunk_id or project, path, and qualified_symbol. Results distinguish exact, likely, and unresolved bindings and may trigger parse-only structural backfill; they never edit source files. This is a syntax-only index: dynamic dispatch, reflection, and files in other languages are invisible to it, so check `limitations` before concluding a declaration is unused.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "$defs": {
          "DeclarationSelector": {
            "description": "One declaration identity, by chunk id or its stable source location.",
            "properties": {
              "chunk_id": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Chunk id from a search_code or find_symbol hit. Cannot be combined with project, path, or qualified_symbol -- provide this alone, or all three of them."
              },
              "project": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Project id, name, or path. Required with path and qualified_symbol."
              },
              "path": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Repo-relative POSIX path to the file holding the declaration (forward slashes, relative to the project root). Required with project and qualified_symbol."
              },
              "qualified_symbol": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "null"
                  }
                ],
                "default": null,
                "description": "Dotted qualified symbol name, e.g. 'Outer.method'. Required with project and path."
              }
            },
            "type": "object"
          }
        },
        "properties": {
          "selector": {
            "$ref": "#/$defs/DeclarationSelector",
            "description": "Declaration selected by chunk id or stable source location."
          },
          "kinds": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "import",
                    "export",
                    "call",
                    "type_use",
                    "inheritance",
                    "decorator",
                    "read",
                    "write"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Optional reference kinds to keep. Omit for all kinds; an unknown kind is rejected rather than silently returning nothing."
          },
          "limit": {
            "default": 100,
            "description": "Maximum results per page.",
            "maximum": 500,
            "minimum": 1,
            "type": "integer"
          },
          "cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Opaque page cursor."
          }
        },
        "required": [
          "selector"
        ],
        "type": "object"
      }
    },
    {
      "name": "find_symbol",
      "title": "Find symbol",
      "description": "Look up indexed code chunks by symbol name, matching exactly, by prefix, or by substring. Returns hits ordered by path and line, each with a snippet and a chunk_id. Matches declaration names only \u2014 not call sites, imports, or other references. For a conceptual query rather than a known name, search_code applies. A root that is not registered yet is registered and indexed before the first query is answered; later queries refresh it when source metadata has changed.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "name": {
            "description": "Symbol name to look up. Either the bare name or the dotted qualified name, for example 'LanceStore' or 'LanceStore.upsert_project'.",
            "type": "string"
          },
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          },
          "match": {
            "default": "exact",
            "description": "How to compare name against stored symbols. 'exact' requires a full match on the bare or qualified name.",
            "enum": [
              "exact",
              "prefix",
              "contains"
            ],
            "type": "string"
          },
          "kinds": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "annotation",
                    "array",
                    "class",
                    "constant",
                    "constructor",
                    "enum",
                    "function",
                    "index",
                    "interface",
                    "method",
                    "module",
                    "object",
                    "property",
                    "record",
                    "signal",
                    "struct",
                    "table",
                    "trigger",
                    "type",
                    "view",
                    "annotation_part",
                    "array_part",
                    "class_part",
                    "constant_part",
                    "constructor_part",
                    "enum_part",
                    "function_part",
                    "index_part",
                    "interface_part",
                    "method_part",
                    "object_part",
                    "property_part",
                    "record_part",
                    "signal_part",
                    "struct_part",
                    "table_part",
                    "trigger_part",
                    "type_part",
                    "view_part"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to these chunk kinds."
          },
          "limit": {
            "default": 20,
            "description": "Maximum hits to return. Hard cap of 50.",
            "maximum": 50,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      }
    },
    {
      "name": "get_chunk",
      "title": "Get chunk",
      "description": "Fetch one indexed chunk's full stored text by the chunk_id returned from search_code or find_symbol, with its path, symbol, and line range. Chunk ids are content-derived and change when the file is re-indexed, so a stale id returns CHUNK_NOT_FOUND rather than the wrong code.",
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "chunk_id": {
            "description": "Chunk id from a search_code or find_symbol hit.",
            "type": "string"
          }
        },
        "required": [
          "chunk_id"
        ],
        "type": "object"
      }
    },
    {
      "name": "index_history",
      "title": "Indexing history",
      "description": "One page of a project's durable indexing history \u2014 each run's id, trigger, server and schema version, embedding model, force flag, start/finish timestamps, final state, phase durations, file and chunk counts, skip counts by reason, bounded error details, and storage table versions before and after. Newest first, paginated with an opaque cursor; at most 100 runs per project are retained and history is never loaded wholesale into project status.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          },
          "cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Opaque cursor from a previous page; omit for the first page."
          },
          "limit": {
            "default": 20,
            "description": "Maximum runs per page, up to 100.",
            "maximum": 100,
            "minimum": 1,
            "type": "integer"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "index_project",
      "title": "Index project",
      "description": "Incrementally index a project: scan for supported source files, parse changed files with Tree-sitter, embed their chunks, and commit them. Files whose size, mtime, and content hash are unchanged are skipped without being re-read. Returns per-phase counts and durations plus any per-file errors. Indexes supported source files, skipping symlinks, binaries, and files over 1 MiB.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          },
          "force": {
            "default": false,
            "description": "Re-parse and re-embed every discovered file, ignoring change detection. Use after changing the embedding model or chunking settings.",
            "type": "boolean"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "index_storage_maintenance",
      "title": "Index storage maintenance",
      "description": "Compact tables and remove verified old Lance versions for one project or the whole installation. Dry-run by default: it reports the before statistics and a labelled reclaimable-bytes estimate, leaving the after statistics null; only an explicit dry_run=false performs cleanup and reports the after statistics, versions removed, bytes reclaimed, duration, skipped projects, and busy projects. Never uses zero-age retention or delete_unverified.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml when exactly one project is in scope; omit for the whole installation."
          },
          "dry_run": {
            "default": true,
            "description": "True (default) reports statistics and a reclaimable-bytes estimate without mutating the index; false performs the cleanup.",
            "type": "boolean"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "index_storage_status",
      "title": "Index storage status",
      "description": "Read-only storage statistics for one project or the whole installation \u2014 current table versions, row counts, Lance-reported logical bytes, filesystem-reported physical bytes, fragment and retained-version counts, index coverage, and an installation total \u2014 plus advisory warnings for overlapping registered roots and Git worktrees that share one repository. Never mutates the index: a registered project with no partition reports zeroed tables instead of materializing one.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml when exactly one project is in scope; omit for the whole installation."
          }
        },
        "type": "object"
      }
    },
    {
      "name": "init_project",
      "title": "Initialize project",
      "description": "Register a directory as an indexable project and write its local .ci-mcp/project.toml marker, which holds a checkout-local id and the scan configuration. Returns the project id, name, root, and scan settings. Building the index is a separate operation (index_project). Re-running on an already-initialized directory returns the existing project unless force_new_id is set. A new registration whose root equals, contains, or is nested inside an existing project's root is rejected unless allow_overlap is true.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": false,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Directory to initialize. Defaults to the single MCP root when exactly one is offered, otherwise to the server's working directory."
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Display name for the project. Defaults to the directory name."
          },
          "force_new_id": {
            "default": false,
            "description": "Mint a new project id even if a marker already exists, orphaning the previous index for this directory.",
            "type": "boolean"
          },
          "allow_overlap": {
            "default": false,
            "description": "Register even when the directory equals, contains, or is nested inside the root of an already registered project, which would index the same sources twice. Set true only for an intentional duplicate registration.",
            "type": "boolean"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "inspect_scan",
      "title": "Inspect scan",
      "description": "One page of a stat-only dry-run scan of a project: what an index run would find, before anything is embedded or written. Each item carries a repository-relative path with the outcome ('eligible' with its language, or 'skipped' with its reason and explanation). Filter by outcome or skip reason and paginate with the opaque cursor. Read-only: never mutates the index and never persists a scan manifest.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          },
          "outcome": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Keep only 'eligible' or 'skipped' items; omit for both."
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Keep only skipped items with this reason: unsupported, ignored, symlink, oversized, or unreadable."
          },
          "cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Opaque cursor from a previous page; omit for the first page."
          },
          "limit": {
            "default": 50,
            "description": "Maximum items per page, up to 200.",
            "maximum": 200,
            "minimum": 1,
            "type": "integer"
          }
        },
        "type": "object"
      }
    },
    {
      "name": "list_projects",
      "title": "List projects",
      "description": "List every project registered with this server \u2014 id, name, root directory, and scan configuration \u2014 sorted by name. Takes no arguments and returns registrations only, not index state; project_status reports that.",
      "annotations": {
        "readOnlyHint": true,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "project_status",
      "title": "Project status",
      "description": "Report one project's index state \u2014 pending, indexing, ready, partial, stale, rebuild_required, or error \u2014 with its indexed file count and chunk count. Compares eligible source metadata with the index but does not rebuild it; index_project does that, including rebuilding a rebuild_required partition. A root that is not registered yet is registered first, which writes its .ci-mcp/project.toml marker.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Project id, name, or path. Defaults to the active MCP root or the nearest .ci-mcp/project.toml."
          }
        },
        "type": "object"
      }
    },
    {
      "name": "remove_project",
      "title": "Remove project",
      "description": "Permanently delete a project's registration and its entire on-disk index partition. The .ci-mcp/project.toml marker in the working tree is left in place, so a later init_project re-registers the same id with an empty index. Irreversible: the only way back is a full re-index. Returns whether a registration existed.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": true,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "project": {
            "description": "Project id, name, or path to remove. Required \u2014 no default.",
            "type": "string"
          }
        },
        "required": [
          "project"
        ],
        "type": "object"
      }
    },
    {
      "name": "search_across_projects",
      "title": "Search across projects",
      "description": "Hybrid semantic and keyword search across an explicit set of related projects for cross-repository debugging. Accepts project ids, unique names, or paths and requires at least two distinct resolved projects. Returns one globally ranked hit list with project metadata; use list_projects first to discover the intended repositories.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "query": {
            "description": "What to look for across the selected projects, as natural language or keywords. Matched against chunk text and normalized identifier names.",
            "type": "string"
          },
          "projects": {
            "description": "At least two project ids, unique names, or paths to search together. Selectors must resolve to at least two distinct projects.",
            "items": {
              "type": "string"
            },
            "minItems": 2,
            "type": "array"
          },
          "languages": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "python",
                    "java",
                    "javascript",
                    "typescript",
                    "tsx",
                    "csharp",
                    "sql",
                    "gdscript",
                    "gdshader",
                    "godot_resource",
                    "yaml",
                    "json",
                    "go",
                    "terraform",
                    "rust",
                    "c",
                    "cpp",
                    "lua"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to these languages across the complete selected scope."
          },
          "paths": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to paths matching these glob patterns relative to each selected project root, for example 'src/*' or '**/*.py'. Patterns match from the right, so '*.py' matches any Python file at any depth."
          },
          "kinds": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "annotation",
                    "array",
                    "class",
                    "constant",
                    "constructor",
                    "enum",
                    "function",
                    "index",
                    "interface",
                    "method",
                    "module",
                    "object",
                    "property",
                    "record",
                    "signal",
                    "struct",
                    "table",
                    "trigger",
                    "type",
                    "view",
                    "annotation_part",
                    "array_part",
                    "class_part",
                    "constant_part",
                    "constructor_part",
                    "enum_part",
                    "function_part",
                    "index_part",
                    "interface_part",
                    "method_part",
                    "object_part",
                    "property_part",
                    "record_part",
                    "signal_part",
                    "struct_part",
                    "table_part",
                    "trigger_part",
                    "type_part",
                    "view_part"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to these chunk kinds across the selected projects."
          },
          "limit": {
            "default": 8,
            "description": "Maximum globally ranked hits to return. Hard cap of 50.",
            "maximum": 50,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "query",
          "projects"
        ],
        "type": "object"
      }
    },
    {
      "name": "search_code",
      "title": "Search code",
      "description": "Hybrid semantic and keyword search over indexed code chunks. Returns hits ranked by relevance, each with a code snippet, file path, line range, and a chunk_id that get_chunk expands to the full text. Searches indexed source only \u2014 not commit history, not comments in unindexed files, and not files excluded by .gitignore or the 1 MiB size cap. For a declaration whose name is already known, find_symbol is direct; for one file's structure, file_outline is cheaper. A root that is not registered yet is registered and indexed before the first query is answered; later queries refresh selected projects when source metadata has changed.",
      "annotations": {
        "readOnlyHint": false,
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false
      },
      "inputSchema": {
        "properties": {
          "query": {
            "description": "What to look for, as natural language or keywords. Matched against chunk text and against normalized identifier names.",
            "type": "string"
          },
          "projects": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict the search to these project ids, names, or paths. Mutually exclusive with all_projects."
          },
          "all_projects": {
            "default": false,
            "description": "Search every registered project. Off by default so results from unrelated repositories are never mixed in implicitly.",
            "type": "boolean"
          },
          "languages": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "python",
                    "java",
                    "javascript",
                    "typescript",
                    "tsx",
                    "csharp",
                    "sql",
                    "gdscript",
                    "gdshader",
                    "godot_resource",
                    "yaml",
                    "json",
                    "go",
                    "terraform",
                    "rust",
                    "c",
                    "cpp",
                    "lua"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to these languages."
          },
          "paths": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to paths matching these glob patterns, relative to the project root, for example 'src/*' or '**/*.py'. Patterns match from the right, so '*.py' matches any Python file at any depth."
          },
          "kinds": {
            "anyOf": [
              {
                "items": {
                  "enum": [
                    "annotation",
                    "array",
                    "class",
                    "constant",
                    "constructor",
                    "enum",
                    "function",
                    "index",
                    "interface",
                    "method",
                    "module",
                    "object",
                    "property",
                    "record",
                    "signal",
                    "struct",
                    "table",
                    "trigger",
                    "type",
                    "view",
                    "annotation_part",
                    "array_part",
                    "class_part",
                    "constant_part",
                    "constructor_part",
                    "enum_part",
                    "function_part",
                    "index_part",
                    "interface_part",
                    "method_part",
                    "object_part",
                    "property_part",
                    "record_part",
                    "signal_part",
                    "struct_part",
                    "table_part",
                    "trigger_part",
                    "type_part",
                    "view_part"
                  ],
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Restrict to these chunk kinds."
          },
          "limit": {
            "default": 8,
            "description": "Maximum hits to return. Hard cap of 50.",
            "maximum": 50,
            "minimum": 1,
            "type": "integer"
          }
        },
        "required": [
          "query"
        ],
        "type": "object"
      }
    }
  ]
}
